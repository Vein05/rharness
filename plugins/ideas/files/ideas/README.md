# Ideas backlog

Ranked by evidence the problem is real and unclaimed. Each idea is one file,
`NN-slug.md`, from `00-template.md`. Novelty scans go stale: **re-scan before
chartering any of these.**

| # | Idea | One-line | Status |
|---|------|----------|--------|

Status values: `candidate`, `chartering` (a `rharness new` project exists),
`deferred` (with a reason), `dead` (moved to the list below).

## Dead cells (do not re-propose)

Ideas confirmed scooped or unworkable, with the date and the closest prior
work that killed them.

| idea | date | killed by |
|---|---|---|
